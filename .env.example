# PostgreSQL Database Configuration
# These defaults work with the Docker setup in docker/docker-compose.yml
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=hathi-db-123!
POSTGRES_DB=hathi_db

# AI Configuration
GOOGLE_AI_API_KEY=your-google-api-key

# Optional: Performance Logging
# Set to true to log performance data to a CSV file
LOG_PERF_TO_CSV=false

# Optional: Chat Logging
# Set to true to log comprehensive chat interactions to CSV files
LOG_CHAT_TO_CSV=false

# Optional: Development Settings
# Set to true to display tool execution information in chat
NEXT_PUBLIC_DISPLAY_TOOL_INFO=false
